# Used automatically by `npm run build` (vite build).
# Points the production bundle at the deployed Render API.
VITE_API_URL=https://dentalappserver.onrender.com/api

# Mixpanel project token — safe to commit, it's a public client-side token
# (it ships inside the JS bundle regardless).
VITE_MIXPANEL_TOKEN=c2a57719f523d610623f05a2ef771a42
